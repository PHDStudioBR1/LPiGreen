# Automatic CI disabled — use DonavanAlencar/ecosystem-monorepo (self-hosted k3s).
name: Disabled — use ecosystem-monorepo
on:
  workflow_dispatch:
jobs:
  notice:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "moved to ecosystem-monorepo"
