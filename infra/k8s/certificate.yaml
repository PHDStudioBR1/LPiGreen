# TLS é gerenciado automaticamente pelo Traefik certResolver (Let's Encrypt)
# via a diretiva tls.certResolver: le no IngressRoute.
#
# Domínios ativos:
#   - lpigreen.546digitalservices.com (prod)
#   - centraldobeneficioenergia.com.br (prod)
#   - representanteigreen.com.br (prod)
#
# NÃO usar cert-manager Certificate para estes domínios
# pois conflita com o certResolver do Traefik (ambos usam HTTP-01 challenge).
