# Permite que o frontend LPiGreen (namespace lpigreen) chame o random-service em phdstudio (prod).
# Aplicar no namespace phdstudio:
#   kubectl apply -f infra/k8s/network-policy-allow-lpigreen-to-random-service-prod.yaml
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-lpigreen-to-random-service-api
  namespace: phdstudio
  labels:
    app: random-service
spec:
  podSelector:
    matchLabels:
      app: random-service
      component: api
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: lpigreen
      ports:
        - protocol: TCP
          port: 3000
