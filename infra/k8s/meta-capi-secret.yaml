# Meta CAPI — token server-side (não commitar valor real).
# kubectl -n lpigreen create secret generic meta-capi-secret \
#   --from-literal=access-token='EAAB...' \
#   --from-literal=test-event-code='' \
#   --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: meta-capi-secret
  namespace: lpigreen
type: Opaque
stringData:
  access-token: ""
  test-event-code: ""
