# API Key para autenticação da API de leads. Gerar valor seguro em produção.
apiVersion: v1
kind: Secret
metadata:
  name: api-secret
  namespace: lpigreen
type: Opaque
stringData:
  api-key: "change_me_api_key"
