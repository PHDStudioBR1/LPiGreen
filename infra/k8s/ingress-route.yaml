# IngressRoute HTTP (redireciona para HTTPS, exceto ACME challenge)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: lpigreen-http
  namespace: lpigreen
spec:
  entryPoints:
    - web
  routes:
    # Demais rotas redirecionam para HTTPS (exceto ACME challenge, tratado pelo solver do cert-manager)
    - match: (Host(`lpigreen.546digitalservices.com`) || Host(`centraldobeneficioenergia.com.br`)) && PathPrefix(`/`) && !PathPrefix(`/.well-known/acme-challenge`)
      kind: Rule
      services:
        - name: web-service
          port: 80
      middlewares:
        - name: redirect-https
          namespace: lpigreen
---
# IngressRoute HTTPS (rotas principais com TLS)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: lpigreen-https
  namespace: lpigreen
spec:
  entryPoints:
    - websecure
  routes:
    # /api/leads (todas as requisições) -> frontend (Next.js): POST do formulário e GET/PATCH/DELETE do CRM
    - match: (Host(`lpigreen.546digitalservices.com`) || Host(`centraldobeneficioenergia.com.br`)) && PathPrefix(`/api/leads`)
      kind: Rule
      services:
        - name: web-service
          port: 80
      middlewares:
        - name: security-headers
          namespace: lpigreen
    # CRM Admin: login, logout, me, export -> frontend (Next.js)
    - match: (Host(`lpigreen.546digitalservices.com`) || Host(`centraldobeneficioenergia.com.br`)) && PathPrefix(`/api/admin`)
      kind: Rule
      services:
        - name: web-service
          port: 80
      middlewares:
        - name: security-headers
          namespace: lpigreen
    # /api/representantes -> frontend (Next.js), usado pelo CRM na edição de lead
    - match: (Host(`lpigreen.546digitalservices.com`) || Host(`centraldobeneficioenergia.com.br`)) && PathPrefix(`/api/representantes`)
      kind: Rule
      services:
        - name: web-service
          port: 80
      middlewares:
        - name: security-headers
          namespace: lpigreen
    # Demais /api (/api/health, /api/aneel/distribuidoras, etc.) -> backend
    - match: (Host(`lpigreen.546digitalservices.com`) || Host(`centraldobeneficioenergia.com.br`)) && PathPrefix(`/api`)
      kind: Rule
      services:
        - name: api-service
          port: 80
      middlewares:
        - name: security-headers
          namespace: lpigreen
    # Frontend (web)
    - match: (Host(`lpigreen.546digitalservices.com`) || Host(`centraldobeneficioenergia.com.br`)) && PathPrefix(`/`)
      kind: Rule
      services:
        - name: web-service
          port: 80
      middlewares:
        - name: security-headers
          namespace: lpigreen
        - name: compress
          namespace: lpigreen
  tls:
    certResolver: le
    domains:
      - main: lpigreen.546digitalservices.com
      - main: centraldobeneficioenergia.com.br
